/* 
  00. Putting a thread into suspended or ready state from running state is called 
      pre-empting a thread.

  00. When a thread is pre-empted the thread scheduler putsthe thread into ready after the
      completion of thread pre-emption.

  00. There are three methods using which Pre-empting of threads is possible
            01. sleep()
            02. yield()
            03. join()
 */

public class _03_00_PreEmpting_threads {
    
}
